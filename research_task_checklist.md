# 跨市场、跨周期“风险溢出网络”研究任务清单（执行版）

## 0. 研究问题与假设预注册（先做）
- **核心问题**：美国能源独立相关地缘政治风险如何在不同时间尺度上传导到能源、股票和大宗商品市场。
- **主假设**：
  1. 高频尺度（2–8天）以“情绪冲击”为主，GPR对股票与油价的净溢出更强；
  2. 低频尺度（32天以上）以“结构重配”为主，能源层成为关键中介；
  3. 重大事件窗口（如制裁升级）下，网络连边密度、系统总溢出和尾部溢出显著抬升。
- 输出：`01_preregistration.md`（变量定义、样本期、事件清单、检验标准）。

## 1. 数据抓取与字典构建（从这里开始）

### 1.1 数据范围与频率
- 频率：日度（主样本），周度（稳健性）。
- 时间区间：建议 2000-01-01 至最新可得日期。
- 时区与交易日：统一到 UTC 日期戳，保留各市场本地交易日并建立“全市场交集日历”。

### 1.2 变量清单（最小可行集）
- **地缘政治层**：
  - GPR 总指数（Caldara-Iacoviello）；
  - 可选：威胁分项、行动分项。
- **能源层**：
  - WTI、Brent、Henry Hub、美国LNG出口相关代理（如可得）；
  - 可选：欧洲TTF、亚洲JKM（体现重心转移）。
- **金融资产层**：
  - 股票：S&P 500、MSCI World、Stoxx Europe 600、MSCI EM；
  - 大宗商品：CRB/Bcom总指数、黄金、铜、小麦（可选）。
- **控制变量（建议）**：
  - 美元指数DXY、VIX、美国10Y国债收益率、联邦基金利率。

### 1.3 数据抓取任务
- 建立 `data_raw/` 与 `data_processed/` 目录。
- 编写抓取脚本：
  - `scripts/fetch_gpr.py`
  - `scripts/fetch_market_data.py`
- 每个脚本输出：
  - 原始文件（保留源格式）；
  - 标准化CSV（`date, variable, value`）；
  - 抓取日志（时间、来源URL、版本号、缺失率）。

### 1.4 数据字典
- 建立 `data_dictionary.csv`：变量名、经济含义、单位、来源、可得起始日、变换方式、缺失处理规则。

## 2. 数据预处理与质量控制

### 2.1 对齐与清洗
- 统一字段名和日期格式；
- 去重、异常值初筛（零价、负价、停牌日误差）；
- 对节假日缺口使用“市场闭市标记”，不直接线性插值。

### 2.2 变换与平稳化
- 价格序列转对数收益率（`r_t = 100*Δln(P_t)`）；
- 指数型风险变量可保留水平并做标准化（z-score）；
- 执行ADF/KPSS/PP检验并记录结果。

### 2.3 肥尾与极端性诊断
- 计算偏度、峰度、Jarque-Bera；
- 极值指数（Hill）或分位数尾部厚度指标；
- 生成“极端事件日”标签（例如收益率绝对值 > 95%分位）。

### 2.4 质量报告（必须产出）
- `reports/02_data_qc_report.md`：
  - 缺失率热力图；
  - 各序列描述统计；
  - 单位根与结构突变初步结果。

## 3. 多尺度分解（MODWT）

### 3.1 参数设定
- 小波基：先用 LA(8) 或 db4；
- 分解层级：J=4~6（对应短中长期）；
- 边界处理：reflection 或 periodic，并做稳健性比较。

### 3.2 输出规范
- 对每个变量输出：`D1...DJ` 与 `S_J`；
- 建立尺度映射表：
  - 高频：D1-D2（短期恐慌）；
  - 中频：D3-D4（政策消化/再定价）；
  - 低频：D5+S5（结构调整）。

### 3.3 检查项
- 能量保持性检查；
- 分解-重构误差<容忍阈值；
- 不同小波基结果的一致性。

## 4. 每个尺度下构建动态溢出网络（TVP-VAR-BK）

### 4.1 模型主线
- 对每个尺度子序列分别估计 TVP-VAR；
- 基于广义预测误差方差分解（GFEVD）计算：
  - 总溢出指数；
  - 方向性溢出（To/From）；
  - 净溢出（Net）；
  - 成对净溢出。

### 4.2 网络化表达
- 节点：所有市场变量 + GPR节点；
- 有向边权：成对溢出强度；
- 滚动输出网络快照（每日/每周）。

### 4.3 核心网络指标
- 全局：密度、平均路径、聚类系数、模块度；
- 节点：出入强度、介数中心性、特征向量中心性；
- 角色识别：风险发出者/接收者/中介节点。

## 5. “三位一体”多层网络耦合

### 5.1 分层建模
- Layer 1：地缘政治层；
- Layer 2：能源市场层；
- Layer 3：金融资产层（股票+大宗商品）。

### 5.2 跨层传导识别
- 计算跨层边占比与强度（L1→L2→L3）；
- 进行路径分解：
  - 直接路径：GPR→股票；
  - 间接路径：GPR→油价→股票/商品。

### 5.3 中介效应检验（建议）
- 在滚动窗口内做动态中介回归或因果图约束检验；
- 对比“含能源中介”与“无中介”模型拟合/预测差异。

## 6. 尾部风险扩展（TENET / 非线性CoVaR）

### 6.1 极端风险建模
- 设定分位点（如 0.05 / 0.95）估计下行尾部溢出；
- TENET或Quantile-VAR/CoVaR用于替代均值溢出。

### 6.2 输出对比
- 均值网络 vs. 尾部网络：
  - 是否出现新关键节点；
  - 是否出现“平时不重要、极端时核心”的节点。

## 7. 事件研究与机制识别

### 7.1 事件清单（需事先固定）
- 美国对主要产油国制裁公告日；
- 俄乌冲突关键节点；
- OPEC+重大决议日；
- 美国出口政策重大变化日。

### 7.2 事件窗分析
- 窗口：[-20,+20]、[-60,+60]；
- 对比事件前后：
  - 总溢出变化；
  - GPR节点净溢出符号变化；
  - 关键路径切换（是否经能源层中介）。

## 8. 稳健性与模型优化

### 8.1 参数稳健性
- 不同VAR阶数、遗忘因子、先验设定；
- 不同小波基、分解层数；
- 不同样本起点与子样本（危机期/平稳期）。

### 8.2 指标稳健性
- 替换风险代理变量（如GPR分项、EPU）；
- 替换资产代表（不同股指、不同商品篮子）。

### 8.3 预测能力检验
- 一步/多步方向溢出预测；
- Diebold-Mariano 比较基准模型。

## 9. 可视化与结果表达
- 多尺度动态网络动画（按时间播放）；
- 三层Sankey图展示传导路径份额；
- 事件前后网络拓扑对比图；
- 节点角色迁移图（sender/receiver regime switch）。

## 10. 最终交付物结构
- `paper/`：论文正文与附录；
- `code/`：可复现实验脚本；
- `data_processed/`：可复现数据快照；
- `reports/`：QC、稳健性、事件研究报告；
- `figures/`：全部图表。

---

## 推荐执行节奏（12周示例）
- 第1-2周：研究设计冻结 + 数据抓取 + 数据字典；
- 第3-4周：清洗、平稳化、极端性诊断；
- 第5-6周：MODWT分解 + 尺度映射；
- 第7-8周：TVP-VAR-BK动态网络；
- 第9周：三层耦合与中介路径；
- 第10周：TENET/CoVaR尾部网络；
- 第11周：事件研究与稳健性；
- 第12周：写作与复现打包。

## 立即可执行的前三步（今天就能开始）
1. **冻结变量清单和样本期**（避免后期“边做边改”导致不可比）。
2. **先完成数据字典与抓取脚本**，保证来源可追溯。
3. **先跑一版最小网络（GPR+WTI+S&P500+Gold）**，打通“分解→建模→可视化”全流程，再扩展全变量。
